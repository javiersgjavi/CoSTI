defaults:
  - base

dataset:
  name: pems-bay
  scenario: block
  scale_window_factor: 1
  batch_size: 16

# Model hyperparameters:
model_config:
  undersampling_factor_time: 1
  undersampling_factor_node: 5

test_sigmas: [80, 1.526]

missing_pattern:
  strategy1: point
  strategy2: block
